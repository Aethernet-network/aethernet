F4A step 2 — replay-conformance template RED baseline
=====================================================

This file captures the failure of TestSyntheticConsumer_ReplayConformance
against the F4A step 2 stub of recognition.ReplayHistoricalToBusConsumers.
The failure IS the contract: F4A step 3 implements the SUT body and the
test must flip from this failure to a clean PASS.

Generated by:
    F4_REPLAY_TEMPLATE_RED=1 go test -race -v \
        -run TestSyntheticConsumer_ReplayConformance \
        ./internal/dispatch/conformance/

Captured output (informational logs trimmed). EventIDs are
content-addressed over fresh random keypairs and will differ on every
run — only the failure SHAPE is load-bearing here:

  - 3 events expected (the corpus consumer is Interested() in 3/3
    Transfer events from makeReplayCorpus).
  - 0 events got (the SUT stub returns nil without emitting).
  - All 3 events appear in the missing diff; the extra diff is empty.

=== RUN   TestSyntheticConsumer_ReplayConformance
=== RUN   TestSyntheticConsumer_ReplayConformance/PopulatedDAGReplay
    replay_path.go:74: PopulatedDAGReplay: Apply did not fire for the expected interested events.
          expected (3): [<id1> <id2> <id3>]
          got      (0): []
          missing  (3): [<id1> <id2> <id3>]
          extra    (0): []
        This is the RED gate — F4A step 3 (recognition.ReplayHistoricalToBusConsumers implementation, F4 plan §8.1) must make this pass.
=== RUN   TestSyntheticConsumer_ReplayConformance/ReplayIdempotent
=== RUN   TestSyntheticConsumer_ReplayConformance/NonInterestedSkipped
--- FAIL: TestSyntheticConsumer_ReplayConformance (6.24s)
    --- FAIL: TestSyntheticConsumer_ReplayConformance/PopulatedDAGReplay (2.04s)
    --- PASS: TestSyntheticConsumer_ReplayConformance/ReplayIdempotent (2.18s)
    --- PASS: TestSyntheticConsumer_ReplayConformance/NonInterestedSkipped (2.02s)
FAIL
FAIL    github.com/Aethernet-network/aethernet/internal/dispatch/conformance    6.551s
FAIL

Notes on the partial PASS state of the other two sub-tests:

  - ReplayIdempotent currently PASSES vacuously: zero Apply invocations
    after the first replay equals zero Apply invocations after the
    second replay. Once F4A step 3 lands, both counts will be 3 and the
    sub-test continues to assert the equality property — no change to
    the assertion is needed.

  - NonInterestedSkipped currently PASSES vacuously: zero Apply
    invocations means the non-matching probe trivially was not applied.
    Once step 3 lands, three Apply invocations fire for the Transfer
    corpus and the probe Generation event must remain unapplied. The
    sub-test assertion (probe not in tracker) holds across both states.

The PopulatedDAGReplay sub-test is the load-bearing RED gate. It moves
from FAIL → PASS the moment recognition.ReplayHistoricalToBusConsumers
walks the DAG and emits each event through the bus.

After step 3:
  - Remove the F4_REPLAY_TEMPLATE_RED skip in replay_path_test.go.
  - Delete recognition.ErrReplayNotImplemented.
  - This baseline file may be retained as F4A historical evidence or
    deleted; the PASSING test suite is the new contract.
